{
  "rules": {
    "rankingsSeason2": {
      ".read": true,
      ".write": true,
      "$record": {
        ".validate": "newData.hasChildren(['name', 'score', 'ts']) && newData.child('name').isString() && newData.child('name').val().length >= 1 && newData.child('name').val().length <= 20 && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('score').val() <= 5000 && newData.child('ts').isNumber() && (!newData.hasChild('mode') || newData.child('mode').val() === 'W' || newData.child('mode').val() === 'S')"
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
